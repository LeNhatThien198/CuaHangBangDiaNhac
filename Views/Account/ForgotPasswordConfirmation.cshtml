@{
    ViewData["Title"] = "Đã gửi email";
    ViewData["HideHeader"] = true;
    var testLink = TempData["TestResetLink"] as string;
}

<div class="row justify-content-center align-items-center min-vh-100 py-5">
    <div class="col-md-5">
        <div class="card shadow-lg border-0 rounded-3 text-center p-5">
            <div class="text-success mb-3"><i class="bi bi-envelope-check-fill" style="font-size: 3rem;"></i></div>
            <h4>Vui lòng kiểm tra Email</h4>
            <p class="text-muted">
                Nếu tài khoản với email này tồn tại, chúng tôi đã gửi hướng dẫn đặt lại mật khẩu.
            </p>

            @if (!string.IsNullOrEmpty(testLink))
            {
                <div class="alert alert-warning mt-4 border-warning">
                    <i class="bi bi-bug-fill me-1"></i> <strong>Email:</strong><br />
                    <a href="@testLink" class="fw-bold text-decoration-underline text-dark stretched-link">
                        Bấm vào đây để đặt lại mật khẩu
                    </a>
                </div>
            }
            <a asp-action="Login" class="btn btn-dark mt-3">Quay về Đăng nhập</a>
        </div>
    </div>
</div>