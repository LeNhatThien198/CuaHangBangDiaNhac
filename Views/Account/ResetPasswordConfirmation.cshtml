@{
    ViewData["Title"] = "Thành công";
    ViewData["HideHeader"] = true;
}

<div class="row justify-content-center align-items-center min-vh-100 py-5">
    <div class="col-md-5">
        <div class="card shadow-lg border-0 rounded-3 text-center p-5">
            <div class="text-success mb-3"><i class="bi bi-check-circle-fill" style="font-size: 3rem;"></i></div>
            <h4>Đổi mật khẩu thành công!</h4>
            <p class="text-muted">Bạn có thể đăng nhập bằng mật khẩu mới ngay bây giờ.</p>
            <a asp-action="Login" class="btn btn-success mt-3">Đăng nhập ngay</a>
        </div>
    </div>
</div>