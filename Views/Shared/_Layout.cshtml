<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CHBDN Store</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/lib/font-awesome/css/all.min.css" />
    <link rel="stylesheet" href="~/lib/bootstrap-icons/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/lib/sweetalert2/sweetalert2.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/user/components.css?v=global_update_22" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/user/home.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/user/product.css?v=global_update_22" asp-append-version="true" />
</head>

@{
        bool hideHeader = (bool?)ViewData["HideHeader"] ?? false;
        string bodyClass = hideHeader ? "no-header" : "has-header";
}

<body class="d-flex flex-column min-vh-100 @bodyClass">

    @if (!hideHeader)
    {
        <header class="fixed-top transition-header">
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark py-3">
                <div class="container">
                    <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                        <i class="fas fa-compact-disc me-2"></i>CHBDN
                    </a>

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navMain">
                        @{
                            var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                            var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
                            var currentCategory = Context.Request.Query["category"].ToString();
                            
                            string GetActiveClass(string controller, string action, string category = null)
                            {
                                if (string.IsNullOrEmpty(category))
                                {
                                    return (currentController == controller && currentAction == action && string.IsNullOrEmpty(currentCategory)) ? "active" : "";
                                }
                                return (currentController == controller && currentAction == action && currentCategory == category) ? "active" : "";
                            }
                        }

                        <ul class="navbar-nav me-auto mb-2 mb-lg-0 fw-bold text-uppercase">
                            <li class="nav-item">
                                <a class="nav-link @GetActiveClass("Home", "Index")" asp-controller="Home" asp-action="Index">Trang chủ</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @GetActiveClass("Product", "Index")" asp-controller="Product" asp-action="Index">Sản phẩm</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @GetActiveClass("Product", "Index", "Cassette")" asp-controller="Product" asp-action="Index" asp-route-category="Cassette">Cassette</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @GetActiveClass("Product", "Index", "CD")" asp-controller="Product" asp-action="Index" asp-route-category="CD">CD</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @GetActiveClass("Product", "Index", "Vinyl")" asp-controller="Product" asp-action="Index" asp-route-category="Vinyl">Vinyl</a>
                            </li>
                            @if (User.IsInRole("Admin") || User.IsInRole("Staff"))
                            {
                                <li class="nav-item nav-item-admin">
                                    <a class="nav-link text-warning" asp-area="Admin" asp-controller="Dashboard" asp-action="Index">Quản trị</a>
                                </li>
                            }
                        </ul>



                        <ul class="navbar-nav align-items-center gap-3 ms-auto">
                            <partial name="_LoginPartial" />
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
    }

    <main class="flex-grow-1 d-flex flex-column">
        <div class="container py-4">
            @RenderBody()
        </div>
    </main>

    <footer class="mt-auto py-5">
        <div class="container">
            <div class="row gy-4">
                <div class="col-lg-4 col-md-6">
                    <h5 class="mb-3"><i class="fas fa-record-vinyl me-2"></i>CHBDN Store</h5>
                    <p class="small text-muted mb-3">
                        Nơi lưu giữ những giai điệu đi cùng năm tháng. Chuyên cung cấp đĩa than, CD, Cassette chính hãng và các ấn phẩm âm nhạc độc lập.
                    </p>
                    <p class="small text-muted">&copy; 2025 CHBDN Store. All rights reserved.</p>
                </div>

                <div class="col-lg-2 col-md-6">
                    <h5>Khám phá</h5>
                    <ul class="list-unstyled text-small">
                        <li class="mb-1"><a href="#">Đĩa than mới về</a></li>
                        <li class="mb-1"><a href="#">Bán chạy nhất</a></li>
                        <li class="mb-1"><a href="#">Pre-order</a></li>
                        <li class="mb-1"><a href="#">Phụ kiện</a></li>
                    </ul>
                </div>

                <div class="col-lg-2 col-md-6">
                    <h5>Hỗ trợ</h5>
                    <ul class="list-unstyled text-small">
                        <li class="mb-1"><a href="#">Hướng dẫn mua hàng</a></li>
                        <li class="mb-1"><a href="#">Chính sách đổi trả</a></li>
                        <li class="mb-1"><a href="#">Vận chuyển</a></li>
                        <li class="mb-1"><a href="#">Liên hệ</a></li>
                    </ul>
                </div>

                <div class="col-lg-4 col-md-6 text-lg-end text-center">
                    <h5 class="mb-3">Kết nối với chúng tôi</h5>
                    <div class="d-flex justify-content-lg-end justify-content-center">
                        <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-youtube"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-tiktok"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/sweetalert2/sweetalert2.all.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/user/product.js" asp-append-version="true"></script>
    <script src="~/js/user/cart.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>